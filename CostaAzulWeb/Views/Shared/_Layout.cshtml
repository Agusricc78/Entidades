<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - Costa Azul</title>

    <!-- Estilos desde wwwroot/css -->
    <link rel="stylesheet" href="~/css/site.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" integrity="sha384-rbsA2VBKQ4fIUqdQ8HU1LT8q8BQT72wOVvM78VPykk+J6H1Ds4P5KhvQA7fMWjHA" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-dark bg-dark">
        <div class="container">
            <!-- Inicio (Primero, con letras blancas) -->
            <a class="navbar-brand text-white" asp-controller="Home" asp-action="Index">Inicio</a>

            <button type="button" class="navbar-toggler" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <!-- Productos (Segundo inicio, redirige a Productos) -->
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Productos" asp-action="Lista">Productos</a>
                    </li>
                    <!-- Sobre Nosotros -->
                    <li class="nav-item">
                        <a class="nav-link" href="#sobreNosotros">Sobre Nosotros</a>
                    </li>
                    <!-- Contacto -->
                    <li class="nav-item">
                        <a class="nav-link" href="#contacto">Contacto</a>
                    </li>
                    <!-- Carrito -->
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Carrito" asp-action="VerCarrito">Carrito</a>
                    </li>
                    <!-- Gestión de Productos (Solo visible si UserId == 1) -->
                    @if (ViewBag.IsLoggedIn != null && ViewBag.IsLoggedIn && ViewBag.UserId == 1)
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Productos" asp-action="Productos">Gestión de Productos</a>
                        </li>
                    }
                </ul>
                <ul class="navbar-nav">
                    <!-- Logout si está logueado -->
                    @if (ViewBag.IsLoggedIn != null && ViewBag.IsLoggedIn)
                    {
                        <li class="nav-item">
                            <form asp-controller="Login" asp-action="Logout" method="post" class="d-inline">
                                <button type="submit" class="btn btn-outline-light">Logout</button>
                            </form>
                        </li>
                    }
                    <!-- Login si no está logueado -->
                    else
                    {
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Login" asp-action="Login">Login</a>
                    </li>
                    }
                </ul>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; @DateTime.Now.Year - Aplicación hecha por SR SOLUTIONS</p>
        </footer>
    </div>

    <!-- Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-kenU1KFdBIe4zVF0s0G1M5b4hcpxyD9F7jL+Xjg3o26ABwzZDFIclw9S6kz8K9a7" crossorigin="anonymous"></script>
    <script src="~/js/site.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    @RenderSection("Scripts", required: false)
</body>
</html>
